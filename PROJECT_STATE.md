# 当前项目状态

本文件回答“项目现在到底是什么状态”。事实仍以当前代码、实际运行结果和 Git 为准；发现冲突时按 `AGENTS.md` 处理。

## 1. 状态基准

- 更新时间：2026-08-24。
- 正式项目路径：`D:\Codex\Projects\tcm-exam-v1`。
- Git 分支：`main`。
- 建档前代码审计基准：`3bc81fbd271fb3757d41dfe66a72dd2402271ca6`；当前实际 HEAD 必须通过 Git 重新确认。
- 建档前工作区：干净，本地 `main` 与 `origin/main` 一致。
- 当前公开测试路径：`https://nolan-here.github.io/tcm-exam-v1/`。
- GitHub 仓库：`https://github.com/Nolan-here/tcm-exam-v1`。
- 项目阶段：复习和分单元考试核心流程已可运行并公开测试；完整题库审计机制已经覆盖当前正式题库，但内容质量 warning、医学专业审核和真人读屏体验仍需继续处理，不能视为正式生产完成。
- 状态内容已经纳入长期上下文文件、完整题库审计模块与测试、README 和 npm 命令调整。它们是否已经提交、推送或部署必须以当前 `git status`、提交历史和远程状态为准，不能只依据本文件推断。

项目还绑定了一个 Sites 测试项目。2026-08-24 只读查询时该项目在线但仅所有者可访问，并非当前公开正式测试路径，也不能据此推断其代码与当前 Git HEAD 相同。

## 2. 当前产品定义

首页当前只提供“复习模式”和“考试模式”两个入口。

### 复习模式

- 可选 10、50、100 或 1—3492 的自定义题量。
- 从完整题库随机抽取，按 A1/A2、A3、B1 分段，完整题组不拆分。
- 每页最多 10 题；题型和题组边界可能使页面少于 10 题。
- 支持首页、上一页、下一页、末页和指定页跳转，可返回已浏览页面。
- 选择答案后只在当前选项末尾更新正确或错误状态，不公布其他选项状态。
- 第一次答错时解析自动展开；第一次答对时解析保持折叠；答对后该题锁定。
- B1 使用共用备选项和题组解析。

复习状态写入 IndexedDB，但首页没有“继续上次复习”入口。刷新或返回首页后，用户不能通过当前 UI 恢复原复习任务。

### 考试模式

- 四个单元分别独立开始；每次选择一个单元完成 150 题，不是一次连续作答 600 题。
- 第一单元：A1/A2 110 题，B1 40 题。
- 第二单元：A1/A2 95 题，A3 17 题，B1 38 题。
- 第三单元：A1/A2 87 题，A3 33 题，B1 30 题。
- 第四单元：A1/A2 80 题，A3 36 题，B1 34 题。
- 完整题组不拆分，同一页面不混合题型，每页最多 10 题。
- 交卷前不显示对错和解析。
- 当前题型确认前可以返回检查；确认后进入下一题型，不能返回修改已锁定题型。
- 允许带未答题确认，确认页显示未答数量。
- 刷新后可以从当前考试题型继续。
- 最终显示总题数、答对数、答错数、错题和解析；B1 保留题组上下文和题组解析。

当前 UI 不提供设置、科目、难度、错题本、收藏、重点题、统计、备份恢复或联网同步入口。

## 3. 当前题库状态

- 题库版本：`2018-2024-pdf-docx-dedup-grouped-v5`。
- 总题数：3492。
- 年份：2018—2024。
- 年份分布：2018 年 162；2019 年 592；2020 年 598；2021 年 565；2022 年 585；2023 年 599；2024 年 391。
- 题型分布：A1/A2 2402；A3 214；B1 876。
- 单元分布：2018 精选单元 162；第一单元 882；第二单元 812；第三单元 846；第四单元 790。
- 题组：1090 道成组题，共 510 组；当前结构扫描未发现题组成员缺失或共用选项冲突。
- 有 155 条解析以“（由AI查询）”结尾，用于区分 AI 补充内容。

### 完整题库审计

- 命令：`npm run audit:bank`。
- 范围：直接读取应用正式汇总入口 `js/questions-bank.js` 的 `QUESTIONS` 导出，即当前应用实际加载的全部题目。
- 范围不是按 2018—2024 或 3492 写死；未来新年份、题目或题组进入正式汇总入口后会自动纳入。
- 2026-08-24 实际扫描：3492 题，年份 2018—2024。
- 实际结果：0 error，102 条 warning，涉及 97 道题。
- warning 分类：70 条缺失占位解析、26 条异常符号、4 条水印、1 条疑似混入下一题题号、1 条明显不可用解析。不同分类可能指向同一道题。
- 其中 23 条异常符号来自 2024 年“【功效】”“【定位】”“【主治】”等语义标题，目前保留为待人工分类的 warning，不能直接认定为题库错误。
- 完整审计已经能够发现此前未进入统一报告的 2023 年“略。”、水印、下一题号和不可用解析问题。

`npm run audit:history` 继续审计 2018—2022 扫描 PDF 来源，当前扫描 2502 题，结果为 0 error、0 warning。它保留该来源特有的 OCR 和原题号规则，只是专项审计，不代表完整题库范围。

### 已确认的内容质量问题

2023 年仍有 70 道题的解析内容只有“略。”：

- 第一单元：017、063、095、117、118、119、120、121、125、126、137、138、145。
- 第二单元：025、027、057、065、098、107、113、115、117、119、121、123、125、127、129、131、133、135、137、139、141、143、147、149。
- 第三单元：044、094、095、106、107、118、119、125、126、135、147、148。
- 第四单元：032、085、088、090、091、093、094、096、097、099、100、102、103、105、106、108、109、111、114、115、133。

另有至少以下明确污染或不可用解析：

- `2023-U1-068`：包含“【一点笔记】”水印。
- `2023-U2-110`：末尾混入下一题题号“111.”和水印。
- `2023-U3-017`：包含 `iji.com` 水印。
- `2023-U4-056`：近似“D略。【一点笔记】”，不具备正常复习价值。

因此，目前至少有 71 道 2023 年题目的解析缺失或不可用。结构完整性、答案字段存在和自动测试通过，都不等于题目答案、解析和医学内容已经由专业教研人员审核。当前没有专业医学审校完成的可靠记录。

## 4. 当前技术实现

- 前端：原生 HTML、CSS 和 JavaScript ES Modules，无 UI 框架。
- 主入口：`index.html` 和 `js/app.js`。
- 数据持久化：浏览器 IndexedDB，数据库名 `tcm-exam-v1`。
- 离线机制：Web App Manifest 和 Service Worker 缓存网页外壳及题库。
- 本地启动：在项目目录执行 `npm start`，访问 `http://127.0.0.1:4173/`。
- 构建：Vite；`npm run build` 用于 Sites/Worker 构建，`npm run build:pages` 生成 GitHub Pages 静态目录。
- 当前正式测试部署：推送 `main` 后由 GitHub Actions 测试并发布 GitHub Pages。

`127.0.0.1` 只供当前 Windows 电脑访问，不能作为 iPhone 局域网测试地址。

## 5. 当前验证状态

### 已自动验证

2026-08-24 当前未提交实现已经实际执行：

- `npm test`：19 项通过，0 项失败；包含完整题库审计入口、动态年份/题型/题组统计、内容警告识别和结构错误分级测试。
- `npm run audit:bank`：读取正式 `QUESTIONS` 导出并扫描 3492 题；0 error，102 条 warning，涉及 97 道题。
- `npm run audit:history`：扫描 2018—2022 来源专项题库 2502 题；0 error，0 warning。
- Microsoft Edge 自动化冒烟检查通过：首页、复习反馈、分页、题组、B1 解析、第二单元考试、题型锁定、刷新恢复、交卷结果和离线重载。
- 公开 GitHub Pages 返回正常，首页无密钥门槛并加载当前 V5 题库标识。
- Worker 的 18 个嵌入资源与当前源文件内容一致。

完整题库审计的 0 error 只表示当前结构性规则没有发现致命问题；102 条 warning 仍真实存在。自动测试和自动内容检查都不等于医学内容已经专业审核。

### 已由真人验证

项目内没有与当前基准提交绑定、可追溯的 NVDA、争渡读屏、VoiceOver 或移动端完整验收记录。此处不能标记为已通过。

### 尚需真人验证

- Windows Edge/Chrome + NVDA。
- Windows Edge/Chrome + 争渡读屏。
- macOS Safari + VoiceOver。
- iPhone Safari + VoiceOver。
- 选项 `aria-label` 更新后是否及时朗读正确/错误状态。
- 解析自动展开后的发现和阅读体验。
- B1 共用题干、选项和解析的浏览效率。
- 题型确认、锁定、未答提示和结果页的理解成本。
- iPhone 添加到主屏幕后首次联网与后续离线启动。

## 6. 当前已知问题

### 高优先级

1. 2023 年至少 71 道题的解析缺失或不可用，并存在下一题号和水印残留。
2. 完整题库审计仍报告 102 条内容质量 warning，其中 23 条 2024 年语义标题警告需要人工判断，其余已确认问题尚未修复。
3. 没有当前版本的真人读屏验收记录。

### 中优先级

1. 复习状态保存，但当前 UI 没有继续复习入口。
2. IndexedDB 加载、写入、容量或隐私模式失败时缺少用户可理解的错误处理。
3. Service Worker 与题库查询版本需要人工同步；新版本激活前可能继续使用旧缓存。
4. 首页自定义题量上限 `3492` 为硬编码，题库变化后需要同步修改。
5. 状态模式名仍为 `review-2024`、`exam-2024`，与 2018—2024 合并题库命名不一致。
6. 历史复习和考试记录没有明确清理策略，状态对象可能随长期使用增长。
7. 大型题库 JavaScript 文件不便人工 diff 和逐题复核。
8. 被忽略的 `tmp` 目录在审计时约 4.84 GB；未经用户批准不得擅自删除。
9. 项目同时保留 GitHub Pages、Sites 和 Worker 托管路径，必须明确 GitHub Pages 才是当前公开测试路径。

## 7. 高回归风险区域

- A3、B1 的整组抽取、共用内容和分页边界。
- 每单元精确 150 题及题型配额。
- 考试题型确认、锁定和刷新恢复。
- 复习第一次答对/答错对应的解析折叠逻辑。
- 单选按钮、`label`、ARIA 名称、状态文字和焦点移动。
- B1 共用解析和考试结果展示。
- OCR 批量修复、去重、ID 和题号对应。
- Service Worker 缓存版本和部署更新。
- 题库总量变化后自定义上限和各考试题池。

## 8. 历史或休眠代码

IndexedDB 默认状态仍保留设置、错题、收藏、重点题、稍后复习、知识点、活动记录和同步字段。项目还存在备份校验、数据合并、同步适配器和旧题库模型；同步适配器明确报告“尚未配置同步后端”。

这些内容没有接入当前 UI，不能认定为当前功能、正在开发的功能或已批准的未来规划。未经新的明确产品决定，不得恢复入口；保留这些字段也不表示可以删除既有用户数据。

## 9. 当前开发优先级

完整题库审计覆盖修正已经完成并通过用户确认。最近开发方向仍集中在缺失解析和 OCR 质量，但开始任何内容修复前应由用户确认具体范围。

审计建议的优先顺序是：

1. 提交当前长期上下文和完整题库审计机制，并用全新 Codex 对话验证项目状态能够从仓库恢复。
2. 后续题库任务先人工分类 warning，再核对并处理 2023 年缺失解析和污染；每次修改前后都运行完整审计。
3. 安排当前版本的 NVDA、争渡和 VoiceOver 真人验证，并记录具体浏览器与结果。

复习恢复、IndexedDB 错误处理和其他产品改动属于候选事项，不代表已经批准开发。

## 10. 下一步建议

1. 用户检查当前未提交 diff；确认无误后进行第一次 Git commit，但不要自动推送或部署。
2. commit 后用全新 Codex 对话按 `AGENTS.md` 完成只读恢复测试，确认它能仅依靠项目文件说清当前功能、审计机制、问题、验证边界和下一步。
3. 恢复测试通过后，由用户明确选择题库质量、无障碍人工验证或其他单一任务。
4. 每个开发阶段结束后，更新本文件的基准提交、验证结果、已知问题和下一步；不要在没有实际证据时预先登记未来功能。
