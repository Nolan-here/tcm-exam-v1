# 当前项目状态

本文件回答“项目现在到底是什么状态”。事实仍以当前代码、实际运行结果和 Git 为准；发现冲突时按 `AGENTS.md` 处理。

## 1. 状态基准

- 更新时间：2026-08-26。
- 正式项目路径：`D:\Codex\Projects\tcm-exam-v1`。
- Git 分支：`main`。
- 长期上下文机制和完整题库审计机制均已建立，并已推送到 `origin/main`。
- 当前 HEAD、`origin/main` 以及本地与远程的 ahead/behind 状态不在本文件中维护；新对话必须通过 `git status`、`git log` 和必要的远程查询实际确认。
- 当前公开测试路径：`https://nolan-here.github.io/tcm-exam-v1/`。
- GitHub 仓库：`https://github.com/Nolan-here/tcm-exam-v1`。
- 项目阶段：复习和分单元考试核心流程已可运行并公开测试；完整题库审计机制已经覆盖当前正式题库，但内容质量 warning、医学专业审核和真人读屏体验仍需继续处理，不能视为正式生产完成。
- 首次全新 Codex 对话只读恢复测试已经完成并通过；新对话能够根据项目文件、Git 和实际代码恢复主要项目状态。任何后续提交、推送、GitHub Actions 或部署状态仍必须通过 Git、实际工作流和公开页面确认，不能只依据本文件推断。

项目还保留 Sites 测试项目配置，但它不是当前公开正式测试路径。Sites 的实际版本、访问状态和部署状态必须实时查询，不能据此推断其代码与当前 Git HEAD 相同。

## 2. 当前产品定义

首页当前保留“复习模式”和“考试模式”两个顶层区域。“复习模式”使用原生可折叠区域，内部提供“随机出题”和可再次折叠的“按科目”。

### 复习模式

- 随机出题可选 10、50、100 或 1—3492 的自定义题量，从年度综合题库抽取。
- 按科目列出 11 个由集中元数据映射生成的正式科目名称；原始 TXT 文件名只用于来源匹配，列表保持上一版稳定顺序。选择后复用相同题量对话框和答题界面，只从该科目抽题。请求数超过科目题数时使用该科目全部题目，不跨科目补题。
- “按科目”继续使用原生 `details/summary`；科目区域是带“选择科目”名称和 `tabindex="-1"` 的 `section`，内部为 11 项 `ul/li/button` 列表。桌面键盘或鼠标主动展开后，在原生 `toggle` 完成后的下一帧聚焦该区域；触摸、初始化、状态恢复和程序化展开不抢焦点。收起时若焦点仍来自科目区域，则返回“按科目”摘要。
- 两种来源都按 A1/A2、A3、B1 分段，完整题组不拆分。
- 每页最多 10 题；题型和题组边界可能使页面少于 10 题。
- 支持首页、上一页、下一页、末页和指定页跳转，可返回已浏览页面。
- 选择答案后只在当前选项末尾更新正确或错误状态，不公布其他选项状态。
- 第一次答错时解析自动展开；第一次答对时解析保持折叠；答对后该题锁定。
- B1 使用共用备选项和题组解析。

复习状态写入 IndexedDB，但首页没有“继续上次复习”入口。刷新或返回首页后，用户不能通过当前 UI 恢复原复习任务。

### 考试模式

- 四个单元分别独立开始；每次选择一个单元完成 150 题，不是一次连续作答 600 题。
- 第一单元：A1/A2 110 题，B1 40 题。
- 第二单元：A1/A2 95 题，A3 17 题，B1 38 题。
- 第三单元：A1/A2 87 题，A3 33 题，B1 30 题。
- 第四单元：A1/A2 80 题，A3 36 题，B1 34 题。
- 完整题组不拆分，同一页面不混合题型，每页最多 10 题。
- 交卷前不显示对错和解析。
- 当前题型确认前可以返回检查；确认后进入下一题型，不能返回修改已锁定题型。
- 允许带未答题确认，确认页显示未答数量。
- 刷新后可以从当前考试题型继续。
- 最终显示总题数、答对数、答错数、错题和解析；B1 保留题组上下文和题组解析。

当前 UI 不提供设置、难度、错题本、收藏、重点题、统计、备份恢复或联网同步入口。

## 3. 当前题库状态

### 科目 TXT 题库

- 本地原始来源实际位于 `D:\中医职医考试题库`；需求文档所写 `D:\中医执医考试题库` 在本机不存在。原始 TXT 保持只读，线上不依赖该绝对路径。
- 发现并成功解析 11 个 TXT，失败 0 个；恢复 13 个旧格式配伍题组后得到 1019 个实际子题。
- 旧解析器曾把“题号.配伍题／【配伍题】+ (1)/(2) 子题”误当成普通 A1/A2，只部署题干“配伍题”和首个答案。当前已从原始 TXT 恢复 13 组、29 个真实 B1 子题；旧 1001 个题目 ID 全部保留，新增 16 个稳定子题 ID。
- 针灸学第 69—70 题与第 96—97 题构成内容完全相同且顺序相反的两个 B1 题组；保留首次出现，排除后续 2 题，实际部署 1017 题。
- 各科目部署题数：中医儿科学 86；中医内科学 76；中医基础理论 82；中医外科学 85；中医妇科学 115；中医诊断学 94；中药学 100；方剂学 100；诊断学基础 100；内科学 78；针灸学 101。
- 缺少答案 0、缺少解析 0、无效题 0。题型分布：A1/A2 785、A3 51、B1 181；共 104 个题组，其中 A3 16 组、B1 88 组。保留 1 条格式异常：针灸学末组标题声明 101—103，实际题号为 102—103；转换按实际两题部署，未修改原 TXT。
- 中医诊断学来源第 29 组的选项“昏睡露晴”已依据大学课程教案和完整题组资料修为“昏睡露睛”；原始 TXT 未改，修订记录和 `before` 防漂移保存在 `data/subject-bank-verified-corrections.json`。
- `npm run audit:subject-quality` 当前扫描 1017 题：占位题干、解析明示答案冲突、重复题答案冲突、相邻解析偏移和未处置候选均为 0。29 个答案/解析启发式候选及 2 组跨题组重复解析均已逐项处置；1 道心衰题因版本口径和题意存在不确定性保留原答案，列入 `data/subject-bank-quality-review.json`。
- 集中名称映射：`scripts/subject-metadata.mjs`；生成模块：`js/questions-subjects.js`；导入脚本：`scripts/import-subject-txt.mjs`；统计报告：`data/subject-bank-import-report.json`；专项审计：`npm run audit:subjects`。11 个既有 `subjectId` 保持不变，正式显示名称依据国家医学考试中心资料，其中两门西医综合采用官方“诊断学基础”和“内科学”。
- 上一版初始 `index.html` 在没有真实异步科目加载的情况下写入“科目列表正在加载”，应用又在等待 IndexedDB 后重建首页，造成 Edge/读屏可能保留失真的初始可访问树。当前初始 DOM 与运行时 DOM 均不再创建该虚假 loading 状态，也不使用 `aria-busy` 或加载文本描述“按科目”。
- 科目题库不追加到年度 `QUESTIONS`，因此原 3492 道综合随机题池和考试配额不变；`getQuestionById` 同时解析年度题和科目题，以复用现有复习会话。

### 年度综合题库

- 题库版本：`2018-2024-pdf-docx-dedup-grouped-v5`。
- 总题数：3492。
- 年份：2018—2024。
- 年份分布：2018 年 162；2019 年 592；2020 年 598；2021 年 565；2022 年 585；2023 年 599；2024 年 391。
- 题型分布：A1/A2 2402；A3 214；B1 876。
- 单元分布：2018 精选单元 162；第一单元 882；第二单元 812；第三单元 846；第四单元 790。
- 题组：1090 道成组题，共 510 组；当前结构扫描未发现题组成员缺失或共用选项冲突。
- 有 221 条解析以“（由AI查询）”结尾，用于区分 AI 补充内容；其中本轮经过证据语义二次复核后保留 66 条 2023 年权威资料后补解析。

### 完整题库审计

- 命令：`npm run audit:bank`。
- 范围：直接读取应用正式汇总入口 `js/questions-bank.js` 的 `QUESTIONS` 导出，即当前应用实际加载的全部题目。
- 范围不是按 2018—2024 或 3492 写死；未来新年份、题目或题组进入正式汇总入口后会自动纳入。
- 2026-08-24 实际扫描：3492 题，年份 2018—2024。
- 2026-08-24 本轮实际结果：0 error，28 条 warning，涉及 28 道题。
- warning 分类：23 条 2024 年异常符号候选、5 条明示保留的 2023 年争议题缺失占位解析。
- 其中 23 条异常符号来自 2024 年“【功效】”“【定位】”“【主治】”等语义标题，目前保留为待人工分类的 warning，不能直接认定为题库错误。
- 完整审计已经能够发现此前未进入统一报告的 2023 年“略。”、水印、下一题号和不可用解析问题。

`npm run audit:history` 继续审计 2018—2022 扫描 PDF 来源，当前扫描 2502 题，结果为 0 error、0 warning。它保留该来源特有的 OCR 和原题号规则，只是专项审计，不代表完整题库范围。

### 2026-08-24 全库质量治理第一阶段（已纳入当前基线）

- 新增独立只读命令 `npm run audit:quality`，动态读取正式 `QUESTIONS`，逐题扫描题干、A—E 选项、解析、题组题干与共用选项；没有修改正式题库。
- 扫描 3492 题，得到 707 条待核对候选，涉及 496 题：题干/题组文字 311 条、选项 29 条、解析 367 条。
- 候选分类：647 条中文词内异常空格、16 条括号失配、10 条题型标签残留、10 条“辨/辩”疑点、9 条水印或来源残留、6 条中文词内孤立 `l/O/o`、5 条字段尾部 OCR 短片段、3 条下一题题号、1 条“肺痿/肺瘘”疑点。
- 解析缺失或不可用仍为 71 题，均来自 2023 年：第一至第四单元分别 13、24、12、22 题。
- 发现 1 组必须单列核对的答案正文冲突：`2019-U3-118` 与 `2021-U3-073` 题干实质相同，但两份原卷选项顺序不同，原答案字母均为 B，正确答案正文分别落在“隔盐灸”和“拔罐”。留存的高分辨率题页和答案页确认这是来源冲突，未修改答案。
- 另列出 841 条高风险原文复核记录，涉及 632 题，包含否定词、首选、禁忌、数字或剂量；这些只是优先复核范围，不代表已经发现错误。
- 后续补充核验已确认原始年度资料位于 `D:\中医职医考试题库`：12 个 PDF、4 个 DOCX；正式 3492 题全部定位到具体 PDF 页或 Word 题号/段落。12 个 PDF 均为纯扫描件；2023、2024 原始 DOCX 存在并可直接读取。该目录中的科目 TXT 不属于当前 2018—2024 正式年度来源。
- 逐条报告位于被 Git 忽略的 `tmp/full-bank-quality-audit/phase1-scan.json`。每条候选记录包含 ID、年份、字段、修改前文字、问题类型、来源文件名、联网状态、置信度和人工审核标记。
- 该阶段涉及 `scripts/full-question-bank-quality-scan.mjs`、`tests/full-question-bank-quality-scan.test.mjs`、`package.json`、`README.md` 和本文件；现已纳入当前基线。

### 2026-08-24 第二阶段第一批来源确认修复（已纳入当前基线）

- 第一阶段 A 类60条候选记录涉及45题、51字段；本批实际处理59条记录、44题、50字段。`2023-U4-056` 同时属于71道缺失或不可用解析，按本批禁止修改缺失解析的边界保持原样。
- 修复通过 `js/source-confirmed-question-repairs.js` 在正式汇总入口逐字段应用；每条同时保存修复前值和修复后值，底层生成题库若发生漂移会立即失败。未改变题目ID、顺序、年份、单元、题型、答案或题组结构。
- 修复类型按候选记录计：括号失配16、题型标签10、辨/辩10、水印或来源噪声8、孤立OCR字符6、尾部垃圾5、下一题题号3、肺痿字形1。
- 647条原文空格没有进行独立删除或规范化。扫描数由647降为646，仅因删除 `2023-U1-068` 整段水印时，水印内部的“一 点笔记”空格随污染一起消失；测试锁定其余646条全部保留。
- 审计规则仅收窄孤立字母 `O` 的边界，避免把 `O139`、`PaCO2` 等字母数字术语误报为OCR字符；真正孤立 `O` 仍会报告。
- 修复后 `npm test` 30项通过；`audit:bank` 为3492题、0 error、96 warning；`audit:history` 为2502题、0 error、0 warning；`audit:quality` 为649条候选、458题，其中646条原文空格、2条来源原文括号候选和 `2023-U4-056` 的1条水印候选，71道缺失解析和1组答案冲突均未改变。
- 逐字段来源、页码或Word位置、修改前后值及排除项记录在被Git忽略的 `tmp/full-bank-quality-audit/phase2-batch1-repair-record.json`。该批代码现已纳入当前基线。

### 2026-08-24：2023 年权威资料后补解析（当前未提交）

- 重新对照正式 `QUESTIONS`、2023 原始 DOCX 和完整质量扫描，确认缺失或不可用解析仍为 71 题：第一至第四单元分别 13、24、12、22 题。
- 原 70 条正式后补经证据语义纠错、全量二次复核和反向证据链核对后，保留 66 题；第一至第四单元分别写入 12、23、11、20 题。后补层仍通过精确 ID 和 `before` 值应用；所有正文以“（由AI查询）”结尾，没有 URL；答案、ID、题型、题组和其他年份未改变。
- 71/71 均有机器可读最终状态。66 条正式记录共引用 73 个唯一来源：S 级 11、A 级 45、B 级 11、C 级 4、辅助来源 2；辅助来源均被明确排除在实质核心来源之外。39 题记录两个以上来源；最终保留的 16 道显式高风险题均由两个独立机构的实质来源支持。
- 5 题未写入解析：`2023-U1-125`、`2023-U2-139`、`2023-U3-148`、`2023-U4-094`、`2023-U4-133`。它们分别存在经典原文与现代口径冲突、病程阶段冲突、具体配穴缺少权威直接支持、高校教学资料与原合方冲突、细分病因定位仅有辅助题库支持等问题。原答案和“略。”均保留，并全部退出正式后补层。
- 法规/伦理题使用 2018 年 10 月 1 日起施行、2023 年仍有效的国务院令第 701 号，没有用 2026 年新规则静默替代。中医教材版本风险在证据记录中逐题标记。
- 机器可读证据：`data/authority-researched-explanations-2023.js`；争议清单：`data/authority-researched-explanation-disputes-2023.js`；正式后补变更：`js/authority-researched-explanation-backfills.js`。
- 自动测试只验证题库结构、证据记录完整性、来源 ID、后补/争议集合一致性、高风险题独立机构数量和 `before` 保护；它不能证明医学语义正确。医学语义结论来自逐题打开正文、正向及反向证据链复核，并在机器可读记录中显式标注 `automatedSemanticValidation: false`。

### 已确认的内容质量问题

- 当前 5 道缺失占位解析均是明示保留的答案或证据争议项；不得为了消除 warning 强行补写或改答案。
- 完整题库另保留 23 条 2024 年语义标题异常符号 warning、645 条原文中文空格候选、2 条已由来源确认保留的括号候选，以及此前确认的 1 组跨年份答案正文冲突；均不在本任务修改范围内。
- 自动测试、结构审计和权威资料检索都不能替代执业医师教研人员对全部医学内容的专业审校。

## 4. 当前技术实现

- 前端：原生 HTML、CSS 和 JavaScript ES Modules，无 UI 框架。
- 主入口：`index.html` 和 `js/app.js`。
- 数据持久化：浏览器 IndexedDB，数据库名 `tcm-exam-v1`。
- 离线机制：Web App Manifest 和 Service Worker 缓存网页外壳及题库。
- 本地启动：在项目目录执行 `npm start`，访问 `http://127.0.0.1:4173/`。
- 构建：Vite；`npm run build` 用于 Sites/Worker 构建，`npm run build:pages` 生成 GitHub Pages 静态目录。
- 当前公开测试发布路径：推送 `main` 后，GitHub Actions 按仓库工作流测试并发布 GitHub Pages；每次运行是否成功以及 Pages 是否完成更新，必须查看实际工作流和公开页面。

`127.0.0.1` 只供当前 Windows 电脑访问，不能作为 iPhone 局域网测试地址。

## 5. 当前验证状态

### 已自动验证

2026-08-26 科目题库错配专项修复已完成本地验证：

- 原始 11 份 TXT 全量解析为 1019 个实际子题；排除已记录的一组 2 题完全重复 B1 题组后部署 1017 题。旧版 1001 个题目 ID 全部保留，新增 16 个子题 ID；13 个旧占位题恢复为 B1 题组成员，既有题目的答案和解析均未改动。
- `npm test`：59 项通过、0 项失败；`audit:subjects`：11 科、1017 题、104 组、0 error、2 warning；`audit:subject-quality`：1017 题、0 error、1 个已登记人工复核 warning、0 未处置候选。
- 年度综合题库独立验证：`audit:bank` 为 3492 题、0 error、28 warning；`audit:history` 为 2502 题、0 error、0 warning；`audit:quality` 为 647 条候选、涉及 457 题、5 道不可用解析、1 组来源答案冲突候选。科目修复未改变年度题库和考试配额。
- `npm run build`、`npm run build:pages` 均成功；本地 Pages 内容指纹缓存为 `tcm-exam-v1-pages-2d69d27e1d62a45e`。真实 Chrome 151 与 Edge 151 的首页、11 科目隔离、复习、考试、持久化、离线完整回归通过；新增指定题组回归验证了两个子题、共用选项、C/D 答案、合并解析和纠错字“昏睡露睛”，控制台错误为 0。
- 自动浏览器和可访问名称检查不等于真人 NVDA、争渡或 VoiceOver 已通过；医学证据不足的 1 道心衰题未猜改，仍需人工专业复核。

2026-08-25 Windows 桌面三个首页入口共同失效修复已完成本地验证：

- 真实 Chrome 151 与 Edge 151 的隔离环境证明当前代码在全新缓存下三条业务链本身正常；桌面 CSS 没有隐藏入口的分支，`subject-panel-focus.js` 不阻止默认事件，也不修改其他面板状态。三个入口分别创建复习或考试会话，但共同依赖 `js/app.js` 成功完成模块初始化、首页重绘和 document 级事件委托。
- 对缺失 `js/subject-panel-focus.js` 的 PWA 缓存进行真实浏览器复现时，旧 `sw.js` 把失败的模块请求回退为 `index.html`。Chrome 和 Edge 均首先报告模块 MIME 类型为 `text/html`；随后 `js/app.js` 整体未执行，静态首页中的科目按钮为 0、随机题量对话框不打开、考试单元为 0，与 Windows 真人描述完全一致。
- 同一旧 Worker 还会在每个 fetch 中删除所有非自身缓存；真实保留 Profile 从 `tcm-exam-v1-20260825-25` 升级时，新缓存会被旧 Worker 竞争删除。当前 `sw.js` 升级到 `tcm-exam-v1-20260825-26`：安装与激活均补全网页外壳，只在激活阶段清理旧缓存，接管后再次校验；只有导航请求允许回退首页，模块和样式请求不得收到 HTML。
- Playwright 高层测试覆盖 Chrome、Edge 的 1366×768 桌面和 390×844 移动视口。随机复习、按科目和第一单元考试都实际进入第一题；11 个科目逐项通过 `display`、`visibility`、`opacity`、bounding box 和 Accessibility Tree 检查，单科首屏题目归属正确，控制台错误为 0。完整浏览器回归还覆盖复习反馈、A3/B1、考试题型锁定、持久化、交卷结果和离线重载。
- `npm test` 为 55 项通过、0 项失败；`audit:subjects` 为 11 科、1001 题、0 error、2 个既有 warning；`audit:bank` 为 3492 题、0 error、28 warning；`audit:history` 为 2502 题、0 error、0 warning；`audit:quality` 为 647 条候选、457 道受影响题、5 道不可用解析和 1 组答案冲突候选。题库数据没有修改。
- `npm run build` 与 `npm run build:pages` 均成功；本地 Pages 内容指纹缓存为 `tcm-exam-v1-pages-dd4afa968bd21789`。真实旧 Profile 无需清除站点数据，关闭并重新打开 Chrome、Edge 后均稳定使用新缓存并跑通三个入口。
- 自动化检查包括 DOM、真实可见性、键盘 Enter/Space 和 Chromium Accessibility Tree，但不等于真人 NVDA、争渡或 VoiceOver 已通过；三种真人读屏仍需用户复测。WebKit 和 iPhone 真机未由本轮自动化控制，已知 iPhone Safari + VoiceOver 正常来自用户提供的真人测试事实。

2026-08-25 科目列表焦点跟随修复已实际执行：

- 根因由代码和运行结构确认：11 个按钮在应用初始化时已经插入 DOM，展开不会触发首页重绘；原生 `details/summary` 保持焦点在摘要，而上一版“选择科目”仅是不可聚焦普通 `div` 的 `aria-label`，也没有列表语义。
- `npm test`：54 项通过，0 项失败；新增 6 项 DOM 焦点测试，覆盖命名区域、11 项列表、`tabindex="-1"`、Enter、Space、鼠标、收起返回摘要、连续三次重新展开、初始化不抢焦点和触摸保护。
- 实现没有 User-Agent 分支、全局 `keydown`、`setTimeout`、live region 或人工切换 `details.open`；科目题库模块未重新生成，11 个稳定 ID、1001 道专项题和 3492 道年度题保持不变。
- `audit:subjects` 为 11 科目、1001 题、0 error、2 个既有 warning；`audit:bank` 为 3492 题、0 error、28 warning；`audit:history` 为 2502 题、0 error、0 warning；`audit:quality` 仍为 647 条候选、457 道受影响题、5 道不可用解析和 1 组答案冲突候选。
- `npm run build` 与 `npm run build:pages` 均成功；本地 Pages 内容指纹缓存为 `tcm-exam-v1-pages-e2d4f8b0b7492c9e`。固定 Service Worker 版本升级到 `tcm-exam-v1-20260825-25`，并预缓存新增焦点模块，旧缓存升级测试通过。
- Chrome 和 Edge 均已安装，但当前未运行且没有安装或启用 ChatGPT 浏览器扩展；因此本轮无法直接控制这两个指定浏览器，也无法读取其 Accessibility Tree。自动 DOM 测试不等于真人 NVDA、争渡或 VoiceOver 验证。

2026-08-25 科目名称与虚假加载状态修复已实际执行：

- `npm test`：48 项通过，0 项失败。新增集中名称映射与永久 loading 状态测试；首页、科目隔离、考试、Pages 和 Service Worker 回归均通过。
- `npm run audit:subjects`：11 科目、1001 道部署题、91 个题组；0 结构错误，2 个如实保留的 warning（1 条源格式异常、排除 2 道完全重复题）。重新生成前后 `subjectId`、题目 ID、题数、题序、题干、选项、答案、解析和题组均未变化，只有标准科目名称与来源元数据变化。
- `npm run audit:bank`：3492 题、0 error、28 warning；`audit:history`：2502 题、0 error、0 warning；`audit:quality`：3492 题、647 条候选、涉及 457 题、5 道争议解析、1 组来源答案冲突，均与修复前基线一致。
- 初始 HTML 与运行时模板均不含“科目列表正在加载”、`aria-busy="true"`、科目专用 `role=status` 或绑定到“按科目”的错误描述。原生 `details/summary`、折叠后隐藏行为和再次展开由自动测试覆盖。
- Windows Computer Use 在激活 Edge 时因无法高置信度识别当前 URL 被安全策略中止；Edge 浏览器扩展连接也不可用。本轮未完成 Microsoft Edge 实际窗口、可访问树或真人 NVDA/争渡/VoiceOver 验证，最终报告必须明确保留该限制，不能用自动测试替代。

2026-08-25 科目分类功能已实际执行：

- `npm test`：46 项通过，0 项失败；新增 7 项科目题库专项测试，并更新首页、Service Worker 和 Pages 测试。
- `npm run audit:subjects`：11 科目、1001 道部署题、91 个题组；0 结构错误，2 个如实保留的 warning（1 条源格式异常、排除 2 道完全重复题）。与年度题库完全相同的题为 0。
- 本地浏览器实际验证：旧 Service Worker 缓存自动升级到新版，无需清站点数据；随机复习答错/答对反馈正常；中医儿科学题抽取并遍历 50 题全部同科目且无重复；切换到中医内科学题1 请求 100 题时只使用该科目全部 76 题；考试第二单元首屏仍为 10 题且交卷前无解析；服务器停止后离线重载首页并显示 11 科目，控制台无错误。
- 原生 `details/summary` 的结构和折叠后隐藏行为已由代码与自动检查覆盖；本轮浏览器控制层无法可靠复现摘要上的 Enter/空格按键事件，真人键盘与读屏仍列为待验证，不能声称已通过真人辅助技术测试。

当前未提交的 2023 年权威资料后补解析已实际执行：

- `npm test`：38 项通过，0 项失败；新增 8 项解析专项测试。
- `npm run audit:bank`：3492 题，0 error、28 warning；其中 23 条为不在本任务处理范围的 2024 年语义标题候选，5 条为明示保留的 2023 年争议题占位解析。
- `npm run audit:history`：2502 题，0 error、0 warning。
- `npm run audit:quality`：3492 题，647 条候选、涉及 457 题；缺失或不可用解析 5 题，均与争议清单一致；645 条原文空格和 2 条来源确认保留的括号候选未处理。
- 专项自动测试已确认 66 个正式变更只涉及解析字段；答案、ID、年份、题型、题组、其他年份及题目顺序均未改变；证据记录和高风险独立机构数量规则均有专项断言，但这些断言不代替医学语义复核。

当前基线中的第二阶段第一批修复曾实际执行：

- `npm test`：30 项通过，0 项失败。
- `npm run audit:bank`：3492 题，0 error、96 warning，涉及 94 题。
- `npm run audit:history`：2502 题，0 error、0 warning。
- `npm run audit:quality`：3492 题，649 条候选，涉及 458 题；其中646条为保留的原文空格，2条为 `2021-U1-086`、`2021-U3-046` 经原始 PDF 确认保留的原文括号候选，另1条为本批排除的 `2023-U4-056` 水印；71题解析缺失或不可用、1组答案正文冲突保持不变。
- 自动候选扫描、OCR 文本或页面图像核对都不等于医学专业正确性验证。

2026-08-24 提交 `da709efabda17a0dc024903cf2432c21bf1a2ebc` 所包含的实现已经实际执行：

- `npm test`：19 项通过，0 项失败；包含完整题库审计入口、动态年份/题型/题组统计、内容警告识别和结构错误分级测试。
- `npm run audit:bank`：读取正式 `QUESTIONS` 导出并扫描 3492 题；0 error，102 条 warning，涉及 97 道题。
- `npm run audit:history`：扫描 2018—2022 来源专项题库 2502 题；0 error，0 warning。
- Microsoft Edge 自动化冒烟检查通过：首页、复习反馈、分页、题组、B1 解析、第二单元考试、题型锁定、刷新恢复、交卷结果和离线重载。
- GitHub Pages 发布链路曾完成自动验证；当前 GitHub Actions 和公开页面状态必须实时检查，不在本文件中维护某次运行编号或部署结果。
- Worker 的 18 个嵌入资源与当前源文件内容一致。

完整题库审计的 0 error 只表示当前结构性规则没有发现致命问题；该历史提交当时的 102 条 warning 不能代表当前结果，当前以本节顶部的 28 条为准。自动测试和自动内容检查都不等于医学内容已经专业审核。

### 项目接管恢复测试

2026-08-24 首次全新 Codex 对话只读恢复测试已通过：

- 新对话成功读取项目级 `AGENTS.md`、`PROJECT_STATE.md`、`README.md` 和当前有效决定。
- 成功核验 Git、实际代码、完整题库范围、题库统计和审计状态。
- 测试发现本文件自身仍把已经提交的实现描述为“未提交”，说明状态摘要可能滞后。
- 该结果再次确认：恢复和接管项目时，必须坚持实际 Git 和代码优先于状态摘要。

### 已由真人验证

项目内没有与当前基准提交绑定、可追溯的 NVDA、争渡读屏、VoiceOver 或移动端完整验收记录。此处不能标记为已通过。

### 尚需真人验证

- Windows Edge/Chrome + NVDA。
- Windows Edge/Chrome + 争渡读屏。
- macOS Safari + VoiceOver。
- iPhone Safari + VoiceOver。
- 选项 `aria-label` 更新后是否及时朗读正确/错误状态。
- 解析自动展开后的发现和阅读体验。
- B1 共用题干、选项和解析的浏览效率。
- 题型确认、锁定、未答提示和结果页的理解成本。
- iPhone 添加到主屏幕后首次联网与后续离线启动。

## 6. 当前已知问题

### 高优先级

1. 5 道 2023 年争议题保留原答案和缺失解析，其中 `2023-U2-139` 为伤寒病程热型冲突，其余四题为本轮语义纠错新增。
2. 完整题库审计最终保留 28 条 warning：23 条 2024 年语义标题候选和 5 道明示保留的 2023 年争议占位解析。
3. 没有当前版本的真人读屏验收记录。

### 中优先级

1. 复习状态保存，但当前 UI 没有继续复习入口。
2. IndexedDB 加载、写入、容量或隐私模式失败时缺少用户可理解的错误处理。
3. Service Worker 的本地源缓存名仍保留显式版本，GitHub Pages 发布缓存名已改由全部发布文件的内容指纹自动生成，并通过旧缓存升级测试防止题库更新后继续复用旧缓存。
4. 题量对话框由当前题池动态设置上限；固定的 10、50、100 按钮在科目题数不足时会自动截断到该科目可用题数。
5. 状态模式名仍为 `review-2024`、`exam-2024`，与 2018—2024 合并题库命名不一致。
6. 历史复习和考试记录没有明确清理策略，状态对象可能随长期使用增长。
7. 大型题库 JavaScript 文件不便人工 diff 和逐题复核。
8. 被忽略的 `tmp` 目录在审计时约 4.84 GB；未经用户批准不得擅自删除。
9. 项目同时保留 GitHub Pages、Sites 和 Worker 托管路径，必须明确 GitHub Pages 才是当前公开测试路径。

## 7. 高回归风险区域

- A3、B1 的整组抽取、共用内容和分页边界。
- 每单元精确 150 题及题型配额。
- 考试题型确认、锁定和刷新恢复。
- 复习第一次答对/答错对应的解析折叠逻辑。
- 单选按钮、`label`、ARIA 名称、状态文字和焦点移动。
- B1 共用解析和考试结果展示。
- OCR 批量修复、去重、ID 和题号对应。
- Service Worker 缓存版本和部署更新。
- TXT 文件名到科目归属、单科目整组抽取和从科目 A 切换到科目 B 的隔离。
- 题库总量变化后自定义上限和各考试题池。

## 8. 历史或休眠代码

IndexedDB 默认状态仍保留设置、错题、收藏、重点题、稍后复习、知识点、活动记录和同步字段。项目还存在备份校验、数据合并、同步适配器和旧题库模型；同步适配器明确报告“尚未配置同步后端”。

这些内容没有接入当前 UI，不能认定为当前功能、正在开发的功能或已批准的未来规划。未经新的明确产品决定，不得恢复入口；保留这些字段也不表示可以删除既有用户数据。

## 9. 当前开发优先级

长期上下文机制和完整题库审计覆盖修正已经完成并推送到 `origin/main`；首次全新 Codex 对话只读恢复测试也已经完成并通过。2023 年缺失解析任务已完成 71/71 的最终状态判定；66 题保留正式后补，5 题进入争议。后续仍集中在争议答案、其余 OCR 质量和专业医学审校，但开始任何新修复前应由用户确认具体范围。

后续开发优先顺序保持为：

1. 后续题库任务优先独立核对 5 道答案/证据争议，或继续人工分类其余 warning；每次修改前后都运行完整审计。
2. 安排当前版本的 NVDA、争渡和 VoiceOver 真人验证，并记录具体浏览器与结果。

复习恢复、IndexedDB 错误处理和其他产品改动属于候选事项，不代表已经批准开发。

## 10. 下一步建议

1. 新对话先通过 `git status`、`git log` 和必要的远程查询确认本地与 `origin/main` 的实际关系；不要从本文件推断当前 SHA 或领先状态。
2. 每次推送 `main` 后，查看实际 GitHub Actions 工作流并访问公开测试页面，分别确认测试、构建和 Pages 部署结果。
3. 后续由用户明确选择题库质量、无障碍人工验证或其他单一任务；未经授权不开始产品开发。
4. 每个开发阶段结束后，更新本文件的验证结果、已知问题和下一步；不要在没有实际证据时预先登记未来功能。
