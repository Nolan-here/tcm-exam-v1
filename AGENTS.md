# 中医执业医师刷题系统 V1：Codex 项目规则

本文件适用于本项目根目录及其所有子目录。它规定 Codex 如何接管、修改和验证项目，不代替产品说明或当前状态记录。

## 新对话接管流程

开始修改前，依次检查：

1. `AGENTS.md`；
2. `PROJECT_STATE.md`；
3. `README.md`；
4. `DECISIONS.md` 中状态为“当前有效”的相关决定；
5. `git status`；
6. 最近相关 Git 提交和当前 diff；
7. 与本轮任务直接相关的代码、数据和测试。

不得只根据聊天历史、上下文摘要、文件名或遗留字段判断项目状态。

## 事实判断原则

事实依据的优先级是：当前实际代码和文件、实际运行行为与可靠测试、Git 状态和 diff、当前项目文档、聊天记录。

`PROJECT_STATE.md` 是快速恢复入口，不能替代对相关代码的核验。如果状态文件与实际代码冲突：

1. 暂停修改；
2. 查明冲突来源；
3. 以当前可靠事实为准；
4. 在本轮任务授权范围内修正状态文档，或者将冲突报告给用户；
5. 不得根据旧聊天强行恢复旧设计。

## 上下文压缩恢复规则

如果当前对话发生过上下文压缩，或者无法确认已完成内容、当前进度和摘要可靠性，不得凭记忆继续。重新读取 `AGENTS.md`、`PROJECT_STATE.md` 和与任务相关的 `DECISIONS.md`，再检查 `git status`、当前 diff、相关源码和必要测试，重建当前任务状态后才能继续。

## 历史功能保护规则

项目中存在历史、休眠或未接入当前 UI 的代码、数据字段和生成文件。它们不能仅因仍然存在就被认定为：

- 当前已实现功能；
- 当前正在开发的功能；
- 已获批准的未来规划。

未经新的明确产品决定，不得恢复设置、错题、收藏、重点题、同步等历史入口，也不得删除历史用户数据。

## 无障碍规则

保持原生 HTML 语义、完整键盘操作、可见焦点、合理焦点管理和读屏兼容性。必须区分：

- 代码层面的无障碍实现；
- 浏览器自动化验证；
- 真人 NVDA、争渡读屏和 VoiceOver 验证。

未经真人实际测试，不得声称某项读屏体验已经通过。修改单选按钮、`label`、ARIA、焦点、状态文字、`dialog`、`details/summary` 或页面切换逻辑时，应把读屏回归列为重点风险，并明确列出需要用户人工验证的行为。

## 题库修改规则

修改题库或导入脚本时，至少检查：

- 题目 ID、年份、单元和题号；
- 题干、A—E 选项、答案和解析；
- A1/A2、A3、B1 题型；
- 题组完整性、共用题干、共用选项和共用解析；
- OCR 错字、残留题号、水印、异常符号和占位内容；
- 去重结果和被连带排除的题组成员；
- 总题量、各单元题池和考试配额是否仍可满足。

自动结构测试通过不等于医学内容正确。无法可靠确认的原文或医学结论必须标记并报告，不能猜测填入。

完整题库审计使用 `npm run audit:bank`。它必须读取当前应用正式汇总入口 `js/questions-bank.js` 实际导出的 `QUESTIONS`，不得把固定年份或固定题数当作永久全库范围。增加新年份、题目或题组时，应先确认它们已进入该正式汇总入口，再确认 `audit:bank` 的动态年份、题型、题组和题数报告已经覆盖新增内容。

`npm run audit:history` 只用于 2018—2022 扫描 PDF 的来源专项规则，不得称为完整题库审计。报告时必须区分结构 `error`、内容质量 `warning`、自动结构验证和医学专业正确性。`npm test` 通过或 `audit:bank` 为 0 error，都不能证明题库没有内容问题或医学结论全部正确；warning 必须如实保留并说明受影响题目。

## 修改范围规则

每个对话优先只处理一个明确任务或一组紧密相关的问题。不要为“顺便优化”修改无关功能。发现其他问题时，记录到 `PROJECT_STATE.md` 或报告给用户；除非它是当前任务完成所必需，否则不要扩大范围。

未经明确授权，不要提交、推送、部署、修改托管权限、删除数据或执行破坏性 Git 操作。不要修改项目目录之外与任务无关的文件。

## 修改后的验证规则

实际修改完成后：

1. 检查 Git diff 和工作区状态；
2. 运行与修改相关的最小必要测试；
3. 检查复习、考试、持久化和离线等受影响的核心流程；
4. 涉及题库时执行相应结构和内容检查；
5. 涉及无障碍时区分自动验证与真人待验证项；
6. 完成一个开发阶段后更新 `PROJECT_STATE.md`；
7. 产生新的长期产品或技术决定时更新 `DECISIONS.md`。

测试命令可能生成或重建被 Git 忽略的文件。只读审计期间应先检查 `package.json` 和相关脚本，不得运行会改变项目目录的完整测试命令。

不得把“相关自动测试通过”描述为“整个产品完全没有问题”。
