# 重要项目决定

本文件只记录未来维护者不应随意推翻、且无法仅从单个代码文件完整理解的决定。状态为“已废弃”的记录只用于防止旧设计被错误恢复。

## 2026-08-26：旧格式配伍题必须恢复为真实 B1 子题

- 状态：当前有效。
- 决定：`scripts/import-subject-txt.mjs` 必须同时支持标准“题号范围+共用选项”和旧式“题号.配伍题／【配伍题】+ (1)/(2) 子题”格式。旧式题组中的每个括号子题分别生成 B1 题目，共享 A—E 选项和题组边界；首个子题沿用旧占位题 ID，后续子题使用 `-S02/-S03` 后缀，使既有 1001 个题目 ID 全部保留。
- 原因：旧解析器把 13 个完整配伍题组压成 13 道题干只有“配伍题”的 A1/A2，丢失 16 个子题并只保留每组聚合答案中的第一个字母。原始 TXT 实际含 29 个子题；恢复后部署题数由 1001 增为 1017，B1 由 152 增至 181。
- 修订边界：原始 TXT 保持只读。来源本身的确认错误通过 `data/subject-bank-verified-corrections.json` 以 `before/after`、稳定目标和来源证据应用；`js/questions-subjects.js` 继续是生成文件。当前首条修订把“昏睡露晴”更正为“昏睡露睛”。
- 验证边界：`audit:subjects` 负责基础结构，`audit:subject-quality` 负责题型占位、B1/A3、答案映射、解析偏移和候选处置。自动检查不能证明全部医学内容正确；证据不足项必须进入 `data/subject-bank-quality-review.json`，不得猜改。

## 2026-08-25：按科目使用原生展开与受约束的焦点进入

- 状态：当前有效。
- 决定：继续保留原生 `details/summary`。科目内容采用带“选择科目”可访问名称、`tabindex="-1"` 的 `section`，内部使用 `ul/li` 表达 11 项真实列表。只有摘要本身已经取得 DOM 焦点、且本次激活不是触摸或笔输入时，才记录一次用户主动展开意图；在原生 `toggle` 确认已经展开后的下一动画帧聚焦科目区域。收起时若焦点仍来自科目区域，则安全返回摘要。
- 原因：Windows Edge/Chrome 的原生展开会继续把焦点留在摘要上；上一版“选择科目”只是普通 `div` 的 `aria-label`，容器不能接收程序化焦点，也没有列表语义。科目按钮在应用初始化时已经插入 DOM，展开过程没有后续重绘，因此根因不是加载或渲染覆盖。
- 移动端边界：不检查 User-Agent，不拦截 Enter/Space，不人工修改 `open`，不使用 `setTimeout`、live region 或加载播报。触摸和笔输入不强制移动焦点，以保护已正常工作的 iPhone Safari + VoiceOver 原生浏览位置；页面初始化、IndexedDB 恢复和程序化展开也不得抢焦点。
- 验证边界：自动 DOM 测试覆盖 Enter、Space、鼠标、收起、连续重新展开、初始化不抢焦点和触摸保护；这些证据不等于 Edge/Chrome 可访问树或真人 NVDA、争渡、VoiceOver 已通过。
- 替代：聚焦第一个科目按钮、把区域加入正常 Tab 顺序、用全局 `keydown` 手工切换 `open`，或按浏览器 User-Agent 分支。

## 2026-08-25：原始 TXT 名、稳定 ID 与正式科目名称分离

- 状态：当前有效。
- 决定：`scripts/subject-metadata.mjs` 是 11 个科目的唯一名称映射来源，集中维护 `sourceName`、原始 `sourceFileName`、稳定 `subjectId`、正式 `subjectName` 和既有显示顺序。重新导入时按原始文件名主体匹配，但运行时 `subject`、`sourceSubject` 和用户界面只使用正式名称；已有 `subjectId` 不变，以兼容 IndexedDB 中可能保存的旧科目会话。
- 正式名称依据：国家医学考试中心《医师资格考试考生指导手册（2023版）》表11明确列出九门中医基础/临床科目，并把西医综合列为“内科学”和“诊断学基础”；国家医学考试中心 2026 年指导用书公告确认当年继续使用 2025 年版中医中西医结合医师资格考试大纲。原文件中的末尾“题”、编号“1”、“方剂学提”和“西依”仅作为来源文件名保留，不进入显示名称。
- 无障碍边界：科目列表来自随应用静态部署的 `js/questions-subjects.js`，没有独立网络加载阶段。初始 HTML 和运行时 DOM 都不得暴露永久“科目列表正在加载”、`aria-busy="true"` 或把加载文本绑定为“按科目”的可访问描述；继续使用原生 `details/summary`。
- 依据链接：`https://www2.nmec.org.cn/wangbao/nme/sp/root/index/2023%E7%89%88%E8%80%83%E7%94%9F%E6%89%8B%E5%86%8C%E6%AD%A3%E6%96%870130.pdf`；`https://www1.nmec.org.cn/Pages/ArticleInfo-14-11859.html`。
- 替代：直接从文件名机械生成显示名称、修改原始 TXT 文件名、或用自定义折叠组件替换原生语义。

## 2026-08-25：科目 TXT 独立题池复用现有复习流程

- 状态：当前有效。
- 决定：首页“复习模式”改为原生 `details/summary` 折叠区域，内部提供“随机出题”和可再次折叠的“按科目”。随机出题继续只使用 3492 道年度综合题库；按科目使用从 11 个 TXT 生成的独立静态题库，但复用同一套复习会话、分页、答题反馈、解析和 IndexedDB 保存逻辑。题目的 `subjectId` 由集中映射保持稳定，`subject` 与 `sourceSubject` 使用正式科目名称；科目内随机不得跨科目补题。
- 原因：科目 TXT 与 2018—2024 年度原卷来源体系不同，直接追加会改变原随机复习和考试题池，并制造与年度题库的潜在重复。独立题池既能保持来源归属，也能保护现有考试配额和综合随机行为。
- 数据边界：原始 TXT 只在开发阶段由 `scripts/import-subject-txt.mjs` 读取；线上运行只加载 `js/questions-subjects.js`。当前解析得到 1019 个实际子题，排除针灸学一组 2 题的完全重复 B1 题组后部署 1017 题；旧 1001 题基线漏掉的配伍子题及修复决定见 2026-08-26 决定。异常与去重证据保存在 `data/subject-bank-import-report.json`。
- 验证边界：`npm run audit:subjects` 和专项测试验证结构、科目隔离、题量与重复；真人 NVDA、争渡和 VoiceOver 体验仍需单独验证。
- 替代：把科目 TXT 直接合并进年度 `QUESTIONS`，或为科目模式复制一套独立答题页面。

## 2026-08-25：GitHub Pages 缓存版本由发布内容指纹生成

- 状态：当前有效。
- 决定：GitHub Pages 构建根据除 `.nojekyll` 外的全部发布文件生成 SHA-256 内容指纹，并将其写入 Pages 专用 Service Worker 缓存名；运行时题库依赖（包括权威后补解析模块）必须进入预缓存清单。新 Worker 在安装和激活阶段都校验并补全当前网页外壳，激活时淘汰旧缓存并接管页面；普通资源请求不再清理其他版本缓存。网络失败时只有导航请求可以回退 `index.html`，JavaScript、CSS 等子资源必须保留真实失败，绝不能收到 HTML。
- 原因：题库入口曾更新而固定缓存名未同步，内容指纹解决了版本识别；但旧实现又在每个 fetch 中删除所有非自身缓存，并把任意失败请求都回退到首页。实测旧 Worker 会与新 Worker 竞争并删除新缓存，缺失模块离线请求则收到 `text/html`，导致整个应用模块初始化失败。把清理限制在激活阶段并按请求类型回退，既避免版本互删，也避免把首页伪装成脚本。
- 验证：Pages 构建测试重新计算内容指纹并核对缓存名；Service Worker 测试模拟安装后缓存被旧 Worker 删除、激活阶段重建，以及离线模块请求不得回退 HTML。真实 Chrome、Edge 隔离 Profile 从上一版缓存升级后，无需清除站点数据，关闭再打开即可由新 Worker 重建当前缓存。线上发布后仍需在公开地址运行三个入口的浏览器测试。
- 替代：每次题库修改时人工同步多个固定版本号，或要求用户手工清除站点数据。

## 2026-08-24：权威后补解析使用独立、可追溯的应用层

- 状态：当前有效。
- 决定：原始题库、来源确认 OCR 修复和联网权威后补解析必须保持职责分离。后补解析通过 `js/authority-researched-explanation-backfills.js` 按题目 ID 和原解析 `before` 值应用，正文统一以“（由AI查询）”结尾；来源、置信度、版本风险和争议另存于 `data/authority-researched-explanations-2023.js`，URL 不进入用户解析。遇到权威资料与答案实质冲突时保留原答案，不强行补写，转入独立争议清单。
- 原因：后补内容不是原题官方解析；独立层能防止底层题库更新后旧补丁静默覆盖新内容，并让每条解析可以回溯到实际打开的资料。争议隔离也避免为了消除占位解析而制造错误医学解释。
- 验证边界：自动测试可以校验证据记录、独立来源数量、后补/争议集合和题库结构，但不能自动证明来源正文在医学语义上支持某个答案；该结论必须来自逐题实际打开正文和正向/反向证据链复核。
- 替代：把联网生成的解析直接写回生成题库、混入 OCR 修复层，或在答案存在冲突时强行解释当前答案。

## 2026-08-24：完整题库范围由应用正式汇总入口定义

- 状态：当前有效。
- 决定：`npm run audit:bank` 必须审计 `js/questions-bank.js` 当前实际导出的 `QUESTIONS`，不得按固定年份或固定题数定义完整题库。来源特有规则可以通过 `npm run audit:history` 等专项命令保留，但不得冒充全库审计。
- 原因：应用正式汇总入口是产品实际使用题目的唯一可靠边界；这样新增年份、题目或题组进入应用后会自动纳入完整审计。error、warning 和医学专业正确性必须分别解释。
- 替代：默认只审计 2018—2022 模块、无法识别完整 `QUESTIONS` 导出且可能静默得到 0 题的旧机制。

## 2026-08-24：长期上下文文件职责分离

- 状态：当前有效。
- 决定：`README.md` 负责稳定产品与使用说明；`AGENTS.md` 负责 Codex 接管和修改规则；`PROJECT_STATE.md` 负责当前真实状态；本文件负责重要长期决定。当前不创建 `PROJECT_SPEC.md`。
- 原因：减少重复维护，让新对话能快速恢复状态，同时仍要求以实际代码、运行结果和 Git 为准。
- 替代：过去主要依赖 README 和聊天上下文恢复项目状态的方式。

## 2026-08-24：首页只保留两个模式入口

- 状态：已废弃；由 2026-08-25“科目 TXT 独立题池复用现有复习流程”取代。
- 决定：首页只显示“复习模式”和“考试模式”。设置、科目、难度、错题本、收藏、重点题、统计、社区和排行榜不属于当前 UI。
- 原因：当前实现和 README 可以确认此结构；更早的原始决策原因缺乏完整、可独立核验的项目内记录。保持简洁有利于读屏用户快速理解页面。
- 替代：包含设置、科目、难度及其他学习管理入口的历史界面。

## 2026-08-24：答案选择依赖原生控件

- 状态：当前有效。
- 决定：使用原生单选按钮、关联 `label` 和浏览器标准键盘行为，不实现 A—E 字母快捷键。
- 原因：当前代码没有字母按键监听；原生语义是当前键盘和读屏兼容性的基础。实际读屏体验仍必须由真人验证。
- 替代：按 A、B、C、D、E 直接选择答案的历史设计。

## 2026-08-24：复习与考试采用不同反馈规则

- 状态：当前有效。
- 决定：复习模式即时更新所选答案的正确或错误状态；第一次答错时解析展开，第一次答对时解析折叠。考试模式交卷前不显示对错和解析。
- 原因：该行为由当前代码、README 和自动化浏览器检查共同确认；原始产品讨论不作为唯一依据。
- 替代：答题后弹出反馈、显示所有选项状态，或者考试过程中即时判分的历史方案。

## 2026-08-24：考试按四个独立单元和题型阶段进行

- 状态：当前有效。
- 决定：四个单元分别独立开始，每次选择一个单元完成 150 题；不是一次连续作答 600 题。题型确认后锁定，不能返回上一题型修改。
- 原因：当前入口、组卷代码、持久化逻辑和浏览器测试均确认该行为。题型锁定用于呈现当前模拟考试流程；原始制度依据未在项目内形成可独立核验文档。
- 替代：按普通分页自由返回全部题型，或者一次连续完成四个单元的方案。

## 2026-08-24：A3 和 B1 必须保持完整题组

- 状态：当前有效。
- 决定：抽题和分页不得拆散 A3、B1 题组；A3 保留共用题干，B1 保留共用备选项。B1 采用题组级解析，避免每个小题重复相同内容。
- 原因：当前题库结构、组卷代码和界面实现依赖题组边界。拆组会丢失必要上下文并降低读屏浏览效率。
- 替代：把题组内各小题当作完全独立题目随机抽取和重复显示解析的旧做法。

## 2026-08-24：GitHub Pages 是当前公开测试路径

- 状态：当前有效。
- 决定：继续使用 `Nolan-here/tcm-exam-v1` 的 `main` 分支和固定 GitHub Pages 地址 `https://nolan-here.github.io/tcm-exam-v1/`。Pages 当前无需访问密钥。
- 原因：当前 Git 远程、部署工作流和在线检查均与该地址一致。固定地址适合长期跨设备测试。
- 替代：临时隧道、一次性测试域名和带密钥的 Pages 访问方式。

Sites 和 Worker 配置仍存在，但不是当前公开正式测试路径。除非用户明确授权，不得发布新版本、修改访问权限或用它们替代 GitHub Pages。

## 2026-08-24：当前学习数据本地保存、当前 UI 不提供同步

- 状态：当前有效。
- 决定：答题数据保存在各自浏览器的 IndexedDB 中；当前 UI 不提供备份、恢复或联网同步入口，也不强制联网。
- 原因：当前页面只调用本地数据库；同步适配器没有后端。离线使用是当前产品实现的一部分。
- 替代：包含备份、恢复、云同步或强制联网入口的历史方案。

遗留数据库字段和同步代码继续存在，不代表相关功能仍在规划中，也不得据此删除历史用户数据。

## 2026-08-24：自动验证不能替代真人读屏或医学审校

- 状态：当前有效。
- 决定：报告中必须分别记录代码检查、浏览器自动化、真人辅助技术测试和医学内容审校。没有对应证据时必须写“尚未验证”。
- 原因：ARIA 和原生控件代码正确不能保证每种读屏的实际朗读体验；题库结构完整也不能证明答案和解析医学正确。
- 替代：以自动测试通过概括整个无障碍体验或题库内容正确的表述。
